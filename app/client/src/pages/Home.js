import React from 'react';
import LoginButton from  '../components/LoginButton';

const Home = () => {
    return (
        <div classNames={'home'}>
            <LoginButton />
        </div>
    );
};

export default Home;